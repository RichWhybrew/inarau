<div class='container theme-showcase' role='main'>
	<div class='jumbotron'>
		<h2>Dojo Leaders</h2>
		<table class='table table-striped table-bordered table-hover table-condensed'>
			<tr>
				<th>Name</th>
				<th>Rank</th>
				<th>Score</th>
			</tr>
			<% @users.each do |user| %>
			<tr>
				<td><%= user.username %></td>
				<td><%= user.belts.last.color %></td>
				<td><%= user.scores.sum(:score) %></td>
			</tr>
			<% end %>
		</table>
		<p>Ready to improve your rank? Click <a href='/katas'>here</a> to continue your training!</p>
	</div>
</div>